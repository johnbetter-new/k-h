import { distance } from 'fastest-levenshtein';
import { normalizePersianText } from './text-normalizer.js';
export interface CourseMatchTarget { id:string; title:string; instructor:string; semester:string; normalizedTitle:string; normalizedInstructor:string; normalizedSemester:string; }
export interface MatchResult { isDuplicate:boolean; confidence:number; matchedCourse?:CourseMatchTarget; reason?:string; }
const TITLES=new Set(['دکتر','مهندس','استاد']);
const tokens=(s:string)=>normalizePersianText(s).split(' ').filter(x=>x&&!TITLES.has(x));
export function matchesInstructor(a:string,b:string):boolean { const x=tokens(a),y=tokens(b); if(!x.length||!y.length)return false; if(x.join(' ')===y.join(' '))return true; if(x.length>1&&y.length>1&&(x.every(t=>y.includes(t))||y.every(t=>x.includes(t))))return true; const aa=x.join(' '),bb=y.join(' '), max=Math.max(aa.length,bb.length); return max>0 && 1-distance(aa,bb)/max>=.9; }
export function detectCourseDuplicate(input:{title:string;instructor:string;semester:string}, existing:CourseMatchTarget[]):MatchResult { const title=normalizePersianText(input.title), sem=normalizePersianText(input.semester); for(const c of existing){ if(c.normalizedSemester!==sem)continue; const im=matchesInstructor(input.instructor,c.instructor); if(!im)continue; if(c.normalizedTitle===title)return {isDuplicate:true,confidence:1,matchedCourse:c,reason:'دقیقاً مطابق با عنوان، استاد و نیم‌سال موجود است.'}; const max=Math.max(title.length,c.normalizedTitle.length); if(!max)continue; const sim=1-distance(title,c.normalizedTitle)/max; if(sim>=.85)return {isDuplicate:true,confidence:sim,matchedCourse:c,reason:`شباهت بالای عنوان (${Math.round(sim*100)}%) با استاد و نیم‌سال یکسان.`}; } return {isDuplicate:false,confidence:0}; }
